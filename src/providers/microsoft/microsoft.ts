import icon from './microsoft.svg';

import type { Provider } from '../providers';

export default {
    key: 'microsoft',
    fontKey: 'windows',
    name: 'Microsoft',
    color: '#00a1f1',
    btnTextColor: '#ffffff',
    icon,
} satisfies Provider;
